<!-- res/menu/drawer_menu.xml -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_new_chat"
            android:title="Chat Baru"
            android:icon="@drawable/ic_new_chat" />
        <item
            android:id="@+id/menu_profile"
            android:title="Profil"
            android:icon="@drawable/ic_profile" />
        <item
            android:id="@+id/menu_settings"
            android:title="Pengaturan"
            android:icon="@drawable/ic_settings" />
        <item
            android:id="@+id/menu_login"
            android:title="Login"
            android:icon="@drawable/ic_login"
            android:visible="false" />
        <item
            android:id="@+id/menu_logout"
            android:title="Logout"
            android:icon="@drawable/ic_logout"
            android:visible="false" />
    </group>

    <item
        android:id="@+id/menu_chat_sessions_heading"
        android:title="Sesi Chat">
        <menu>
            <!-- Dynamic chat session items will be added here programmatically -->
        </menu>
    </item>

</menu>
